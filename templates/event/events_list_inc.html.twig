{% extends 'base.html.twig' %}

{% block body %}


    <div class="search-bar p-2 m-3">
        <form class="form-horizontal" method="post" accept-charset="utf-8">  <!-- barre de recherche Homepage -->
            <div class="input-group recherche">
                <i class="fas fa-search fa-2x pl-3 my-auto"></i>
                <input type="text" id="search-input" autocomplete="off" value=""
                       class=" form-control border-none search-input"
                       placeholder="Que recherchez-vous?">
                <div class="btn-container">
                    <button type="submit" class="btn-custom">Rechercher</button>
                </div>
            </div>
        </form>
    </div>

    {# Row results loop by instant search #}
    <div class="row">
        <div id="hits"> <!-- Instantsearch.js -->
            <script type="text/html" id="hit-template">
                <div id="hit">
                    <a class="card cardlink"
                       href="{{ path('root') }}event/{% verbatim %}{{{ objectID }}}{% endverbatim %}">
                        <img class="card-img-top hit-image"
                             src="{% verbatim %}{{{image}}}{% endverbatim %}"
                             alt="">
                        <div class="card-body pl-0" id="hit-content">
                            <p class="card-subtitle text-muted ">{% verbatim %}{{{_highlightResult.place.value}}}{% endverbatim %}</p>
                            <h4 class="card-title">{% verbatim %}{{{_highlightResult.name.value}}}{% endverbatim %}</h4>
                            <p class="card-text">{% verbatim %}{{{_highlightResult.description.value}}}{% endverbatim %}</p>
                            <p class="card-subtitle text-muted">Réservation</p>
                        </div>
                    </a>
                </div>
            </script>
        </div>
    </div>




    </div>
    <script src="{{ asset('js/app2.js') }}"></script>
{% endblock %}