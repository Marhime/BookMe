{% block modalLogin %}    
      <!-- Modal -->
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
            <h1 class="modal-title" id="LoginModalLabel">Connexion</h1>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
            <h5 class="modal-body">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <h5 class="modal-title" id="LoginModalLabel">Vous êtes déjà connecté</h5>
                    <h2><small>{{ app.user.username }}</small> Ce n'est pas vous ? <a href="{{ path('logout') }}">Se déconnecter</a></h2>
                    {% else %}
                    {% if last_username %}
                    <h5 class="modal-title" id="LoginModalLabel">Ravi de vous revoir</h5>
                    {% endif %}
                    <form action="{{ path('login') }}" method="post" class="col-lg-8 mx-auto pt-4">
                        {% if error %}
                            <h5 class="modal-title" id="LoginModalLabel">{{ error.messageKey|trans(error.messageData, 'security') }}</h5>
                        {% endif %}
                        <div class="form-group">
                                <label class="mb-0" for="username"><small>Adresse email</small></label>
                                <input type="email" class="form-control" id="username" name="_username" aria-describedby="emailHelp" value="{{ last_username }}" placeholder="exemple@mail.fr">
                            </div>
                            <div class="form-group">
                                <label class="mb-0" for="password"><small>Mot de passe</small></label>
                                <input type="password" class="form-control" name="_password" id="password" placeholder="Mot de passe">
                            </div>
                            <div class="text-center pt-2">
                                <input type="hidden" name="_target_path" value="/home" />
                                <button class="btn btn-danger btn-lg" type="submit"><span class="fa fa-user fa-lg pr-2"></span>Se connecter</button>
                            </div>
                        </form>
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
              <p class="text-center">Vous n'avez pas de compte ? <a data-dismiss="modal" data-toggle="modal" href="" data-target="#RegisterModal">Inscription</a></p>
            </div>
          </div>
        </div>

{% endif %}
{% endblock %}